fastapi[standard]>=0.118.2
uvicorn
google-generativeai>=0.7.2
pydantic

# Heavy optional dependencies (chromadb, sentence-transformers, instructor)
# are now optional in the code. Install them locally if needed:
# pip install chromadb>=0.5.4 sentence-transformers>=3.0.1 instructor>=1.3.3